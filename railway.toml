[build]
builder = "nixpacks"
buildCommand = "cd app && npm install --omit=dev --ignore-scripts"

[deploy]
startCommand = "cd app && node server/src/index.js"
healthcheckPath = "/api/tenants"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
